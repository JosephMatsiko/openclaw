{
  "name": "@openclaw/skill-self-improvement-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Recursive autonomy primitive: scans Chuck's state for 6 gap categories and drops docket tasks to fix them. Idempotency via fingerprint dedupe.",
  "type": "module",
  "dependencies": {
    "typebox": "1.1.33"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
