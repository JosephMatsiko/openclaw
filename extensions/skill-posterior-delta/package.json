{
  "name": "@openclaw/skill-posterior-delta",
  "version": "0.1.0",
  "private": true,
  "description": "Schema-validated, append-only posterior-delta writer for chuck-v3 family runner receipts. Converts runner-execution JSON into per-surface posterior deltas (CLAIMS/RISKS/EVIDENCE/RECOMMENDATIONS) plus optional read markers when a prior was injected. Salvages chuck-posterior-delta.mjs.",
  "type": "module",
  "dependencies": {
    "ajv": "^8.20.0",
    "typebox": "1.1.33"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
