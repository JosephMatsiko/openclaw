{
  "name": "@openclaw/skill-gateway-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Polls openclaw gateway event-loop health and auto-restarts via launchctl when pinned past threshold. Anti-flap backoff + daily cap. Long-running daemon stays in .mjs until openclaw cron supports it.",
  "type": "module",
  "dependencies": {
    "typebox": "1.1.33"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
