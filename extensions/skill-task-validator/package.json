{
  "name": "@openclaw/skill-task-validator",
  "version": "0.1.0",
  "private": true,
  "description": "Post-spawn deliverable validator: per-commandKind heuristic checks (existence, size, freshness, syntactic parse) plus LLM-introspection for build-class tasks.",
  "type": "module",
  "dependencies": {
    "typebox": "1.1.33"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
