{
  "name": "@openclaw/plugin-web-push",
  "version": "0.1.0",
  "private": true,
  "description": "Web Push (RFC 8030) sender + VAPID + subscription store for the chuck-v3 PWA. Wraps the `web-push` npm lib.",
  "type": "module",
  "dependencies": {
    "typebox": "1.1.33"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
