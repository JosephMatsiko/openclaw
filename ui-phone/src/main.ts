// Entry point. Importing the element registers <chuck-app> with the global
// customElements registry; the element declared in index.html renders
// once the module graph resolves.
import "./app.ts";
